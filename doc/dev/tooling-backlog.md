# Tooling backlog → moved to the issue tracker

This file is **retired**. The "manual one-shots → proper tooling" backlog now
lives in the forge issue tracker (`botfam/botfam`), so each item has one
authoritative home:

- #2 — Forgejo-in-Docker target + scripted provisioning (integration-test substrate)
- #3 — Forge port + `ccrep-merge-gate` status auto-poster (botfam-next)
- #4 — forge git-push credential helper (token-file)
- #5 — `botfam forge setup` (per-agent onboarding)
- #6 — `merge-gate` auto-resolves the fam ledger (drop manual `COLLAB_HISTORY`)
- #7 — `botfam verify <sha>` (ephemeral-worktree build+test)
- #8 — fold `internal/ccrep` TEMPNOTEs + commit the core
- #9 — `external-review` skill (wrap `external-review.sh` + consolidation subagent)
- #10 — `ccrep-integration-test-v1` (two-tier: BOOTSTRAP fam → botfam-next)
- #11 — `botfam-server-v1` (auth/UI front-end, design)

New tooling/bug items go to the tracker, not here.
