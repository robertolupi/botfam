.gocache/
.gomodcache/
# Workspace-local caches used by managed harness verification.
.cache/
/botfam
.claude/settings.local.json
.mcp.json
.mcp.json.lock
scratch/
# The top-level synced wiki clone only — anchored so it does not also ignore
# the internal/wiki Go package.
/wiki/
doc/collab/history.jsonl
.DS_Store
.obsidian/
